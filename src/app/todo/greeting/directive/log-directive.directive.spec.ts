import { LogDirectiveDirective } from './log-directive.directive';

describe('LogDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LogDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
